version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging into Docker Hub...
      - echo "$DOCKERHUB_PASSWORD" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin

      - echo Logging into ECR in all target regions...
      - REGIONS="us-east-1 ap-southeast-1"
      - |
        for REGION in $REGIONS; do
          echo "Logging into ECR in $REGION..."
          aws ecr get-login-password --region $REGION | \
            docker login --username AWS --password-stdin 722136401996.dkr.ecr.$REGION.amazonaws.com || exit 1
        done

  build:
    commands:
      - set -e
      - echo Building Docker image...
      - docker build -t example-nodejs .

      - echo Tagging and pushing Docker image to ECR in all regions...
      - |
        for REGION in $REGIONS; do
          IMAGE_URI="722136401996.dkr.ecr.$REGION.amazonaws.com/example-nodejs:latest"
          docker tag example-nodejs:latest $IMAGE_URI
          docker push $IMAGE_URI
        done

      - echo Deploying CloudFormation stacks in all regions...
      - |
        for REGION in $REGIONS; do
          echo "Deploying VPC Stack in $REGION..."
          aws cloudformation deploy \
            --region $REGION \
            --stack-name vpc-stack-$REGION \
            --template-file infra/vpc_stack.yaml \
            --capabilities CAPABILITY_NAMED_IAM || exit 1

          echo "Deploying ALB Stack in $REGION..."
          aws cloudformation deploy \
            --region $REGION \
            --stack-name alb-stack-$REGION \
            --template-file infra/alb_stack.yaml \
            --capabilities CAPABILITY_NAMED_IAM || exit 1

          echo "Deploying ECS Stack in $REGION..."
          aws cloudformation deploy \
            --region $REGION \
            --stack-name ecs-stack-$REGION \
            --template-file infra/ecs_stack.yaml \
            --capabilities CAPABILITY_NAMED_IAM \
            --parameter-overrides \
              KeyName=$KEY_NAME \
              TargetGroupExportName=CMS3-TG-Nodejs-$REGION || exit 1

          if [ "$REGION" = "us-east-1" ]; then
            echo "Deploying CloudFront + WAF Stack globally from $REGION..."
            aws cloudformation deploy \
              --region $REGION \
              --stack-name waf-cloudfront-stack \
              --template-file infra/waf_cloudfront_stack.yaml \
              --capabilities CAPABILITY_NAMED_IAM \
              --parameter-overrides RegionName=$REGION || exit 1
          fi
        done

artifacts:
  files:
    - '**/*'
